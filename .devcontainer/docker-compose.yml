version: '3.8'

services:
  db:
    image: postgres:latest
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: quantuser  # Replace with your desired username
      POSTGRES_DB: pg_qdata    # Replace with your desired database name
      POSTGRES_PASSWORD: Chi3fd0++ # Replace with a strong password

volumes:
  postgres-data:
